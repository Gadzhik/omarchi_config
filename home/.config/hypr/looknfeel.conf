# Change the default Omarchy look'n'feel

# https://wiki.hypr.land/Configuring/Basics/Variables/#general
general {
    # No gaps between windows or borders
    gaps_in = 1
    gaps_out = 1
    border_size = 2

    # Red border on the focused window. Overrides $activeBorderColor from the
    # theme (rgb(7aa2f7), Tokyo Night blue) — this file is sourced after
    # ~/.config/omarchy/current/theme/hyprland.conf, so it wins.
    #
    # Gradient red -> neon pink at 45°, matching the terminal palette. For a
    # flat colour instead: col.active_border = rgb(ff1744)
    col.active_border = rgba(ff1744ff) rgba(ff4f9aff) 45deg

    # Change to niri-like side-scrolling layout
    # layout = scrolling
}

# The theme also paints grouped (tabbed) windows with $activeBorderColor, so
# without this a group would keep the blue border and look unfocused.
group {
    col.border_active = rgba(ff1744ff) rgba(ff4f9aff) 45deg
}

# https://wiki.hypr.land/Configuring/Basics/Variables/#decoration
decoration {
    # Use round window corners
    # rounding = 8

    # Dim unfocused windows (0.0 = no dim, 1.0 = fully dimmed)
    # dim_inactive = true
    # dim_strength = 0.15
}

# https://wiki.hypr.land/Configuring/Basics/Variables/#misc
misc {
    # Adaptive sync (VRR): 0 = off, 1 = on, 2 = only for fullscreen apps
    vrr = 2
}

# https://wiki.hypr.land/Configuring/Basics/Variables/#animations
animations {
    # Disable all animations
    # enabled = no

    # Apply opacity changes instantly instead of fading over ~300ms.
    # Without this, window-peek (SUPER+ALT+BACKSPACE) ramps in slowly.
    animation = fadeSwitch, 0, 1, default
}

# https://wiki.hypr.land/Configuring/Basics/Variables/#layout
layout {
    # Avoid overly wide single-window layouts on wide screens
    # single_window_aspect_ratio = 1 1
}

# https://wiki.hypr.land/Configuring/Layouts/Scrolling-Layout/
scrolling {
    # See only one column per screen instead of two
    # column_width = 0.97
}

# Low-battery warning window from battery-low-warn: big, centred, above
# everything, and present on whatever workspace you happen to be on.
windowrule = float on,      match:class ^battery-warning$
windowrule = size 1100 640, match:class ^battery-warning$
windowrule = center on,     match:class ^battery-warning$
windowrule = pin on,        match:class ^battery-warning$
