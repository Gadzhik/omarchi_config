# Official-repo packages (installed with pacman / omarchy pkg add)
# One package per line; lines starting with # are ignored.

# --- IDEs / editors (JetBrains Community) ---
intellij-idea-community-edition
pycharm-community-edition

# --- Languages / runtimes ---
jdk21-openjdk
go
# Node.js is managed by mise (see install.sh), not a repo package.
python-pipx          # Arch marks the system Python externally-managed, so `pip install --user`
                     # refuses to run. pipx gives each CLI app its own venv under
                     # ~/.local/share/pipx/ and symlinks only the entry point into
                     # ~/.local/bin, which is already on PATH. Used for sherlock-project.

# --- Databases ---
postgresql
dbeaver

# --- AI / local LLM ---
ollama

# --- File manager: Thunar (default, replaces Nautilus) + plugins ---
thunar
thunar-volman            # auto-mount removable media
thunar-archive-plugin    # create/extract archives from context menu
thunar-media-tags-plugin # edit audio tags, rename by tags
tumbler                  # thumbnail service
ffmpegthumbnailer        # video thumbnails
gvfs                     # trash, mounting, network
gvfs-mtp                 # Android / MTP devices
gvfs-gphoto2             # cameras
gvfs-smb                 # Windows/SMB shares
gvfs-afc                 # iOS devices
gvfs-nfs                 # NFS shares
xarchiver                # backend for thunar-archive-plugin
catfish                  # file search

# --- Media ---
vlc
ffmpeg

# --- yt-dlp + all optional dependencies ("со всеми дополнениями") ---
yt-dlp
aria2
atomicparsley
rtmpdump
python-mutagen
python-pycryptodomex
python-brotli
python-websockets

# --- Networking / VPN ---
openvpn

# --- Network analysis ---
wireshark-qt         # Wireshark GUI (pulls wireshark-cli); add user to 'wireshark' group to capture as non-root
tcpdump

# --- Shell: zsh + plugins (default login shell; see home/.zshrc) ---
zsh
zsh-autosuggestions
zsh-syntax-highlighting
zsh-completions
zsh-history-substring-search

# --- Containers ---
docker
docker-compose

# --- Virtualization: KVM/QEMU + virt-manager GUI (+ UEFI/TPM for guests) ---
qemu-desktop
libvirt
virt-manager
dnsmasq
edk2-ovmf
swtpm

# --- Kubernetes: local cluster + tooling ---
minikube
kubectl              # from the kubernetes-tools package split
helm
k9s                  # TUI dashboard for the cluster

# --- Automation ---
ansible
ansible-lint
